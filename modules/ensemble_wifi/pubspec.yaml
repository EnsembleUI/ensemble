name: ensemble_wifi
description: Programmatic WiFi connection support for Ensemble apps.
homepage: https://ensembleui.com
repository: https://github.com/EnsembleUI/ensemble
issue_tracker: https://github.com/EnsembleUI/ensemble/issues
documentation: https://github.com/EnsembleUI/ensemble/tree/main/modules/ensemble_wifi#readme
topics:
  - wifi
  - connectivity
  - low-code
  - ensemble
version: 1.2.48

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  ensemble:
    git:
      url: https://github.com/EnsembleUI/ensemble.git
      ref: ensemble-v1.2.48-beta.2
      path: modules/ensemble
  plugin_wifi_connect: ^2.0.1
  geolocator: ^9.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
