import 'package:flutter_test/flutter_test.dart';

import 'package:ensemble_bluetooth/ensemble_bluetooth.dart';

void main() {
  test('adds one to input values', () {
    final bluetooth = BluetoothManagerImpl();
    // bluetooth.init();
    
  });
}
