name: wallet
description: A sample command-line application.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  path: ^1.8.0

  # WalletConnect
  ensemble_walletconnect:
    path: ../../

  # Algorand
  algorand_dart: 0.0.2-beta.8

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
