export interface BaseStyles {
  /**
   * 	If the parent is a Row or Column, this flag will stretch this widget in the appropriate direction.
   * 	(e.g stretch horizontally for parent of type Row)
   */
  expanded?: boolean;
}
